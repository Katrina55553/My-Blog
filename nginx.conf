server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # static assets with long cache
    location /_astro/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # sitemap, robots, rss
    location ~ ^/(sitemap|robots|rss) {
        try_files $uri =404;
        expires 1h;
        add_header Cache-Control "public";
    }

    # Umami API proxy — inject API key server-side
    location /api/views/ {
        proxy_pass https://cloud.umami.is/api/;
        proxy_set_header x-umami-api-key "api_m3AASQAcaRj3ca3M9p6x1z6orcoeapNv";
        proxy_set_header Host cloud.umami.is;
    }

    # html pages with no cache (for content updates)
    location / {
        try_files $uri $uri.html $uri/index.html =404;
        expires -1;
        add_header Cache-Control "no-cache";
    }
}
